#include <stdio.h>
int main(int argc, char const *argv[])
{
	//find minimum distance from source to destination
	//initially add source vertex to done list

	// add vertex to done list
	//add all its neighbours to undone list-
	//remove min-distance vertex from undone list
	// add it to done list
	//repeat

	struct node {
		int vertex;
		int distance;
	};

	
	return 0;
} 